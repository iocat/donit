# Package models contains project's models #
Every model has to take a context uuid for the purpose of logging. The models 
also has to comply with the error package to allow the client code optionally
handle the error